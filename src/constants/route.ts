export enum ROUTE {
  HOME = '/',
  LOGIN = 'login',
  COUPLE_DEBT = 'couple-debt',
  CURRENCY_RATE = 'currency-rate',
  DAILY_ROUTINE = 'daily-routine',
}

export enum ROUTE_PUBLIC {
  HOME = '/shared',
  CURRENCY_RATE = 'currency-rate',
}
